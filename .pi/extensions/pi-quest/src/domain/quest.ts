export function recordAdvisoryNote(): void {
  // S3: record setback reason + evidence; blocks nothing.
}

export function validateThenArchiveOrDemote(): void {
  // S3: dual-scope validation verdict handling.
}

export function recordChildFindings(): void {
  // S3: record returned findings; amend or explicitly continue past.
}

export function applyLateAnswer(): void {
  // S3: apply a late user answer as refinement or confirmation.
}
