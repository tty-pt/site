// HIGH_LEVEL: #working together
// HIGH_LEVEL: #human absence — ask-with-default + timeout race.
// SPEC: B1.9.
import type { ExtensionAPI } from "../index.ts";

export function installHumanAbsence(_pi: ExtensionAPI): void {
  // S3: ask_human timeout/default/late-answer semantics land here.
}
