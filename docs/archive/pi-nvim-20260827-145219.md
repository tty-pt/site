# Task: Clean Neovim `init.lua` and Integrate `bamggm/pi.nvim`

## Goal
- Clean and simplify `~/.config/nvim/init.lua`.
- Replace `pablopunk/pi.nvim` and `codecompanion.nvim` with `bamggm/pi.nvim` (https://github.com/bamggm/pi.nvim) powered by `folke/snacks.nvim`.
- Add auto-reload (`checktime`) hooks for buffer synchronization when Pi edits files on disk.
- Clean up which-key definitions, remove dead code/redundancies, and verify headless loading.
- Stage `~/.config/nvim/init.lua` and `~/.tmux.conf` to `~/.dotfiles.git`, commit, and push to GitHub.

## Current Status
- Completed and verified.
- `folke/snacks.nvim` and `bamggm/pi.nvim` installed and configured.
- Legacy `codecompanion.nvim`, `plenary.nvim` (agent use), and `pablopunk/pi.nvim` cleanly removed.
- All commands (`:PiOpen`, `:PiSendFile`, `:PiSendSel`) verified in Neovim runtime.
- Committed and pushed to `git@github.com:quirinpa/dotfiles.git` on `main` branch.

## Decisions made
- Replaced legacy AI plugins with `bamggm/pi.nvim` + `folke/snacks.nvim`.
- Configured right split terminal window with `width = 0.40` for spacious agent diff/chat display.
- Added `checktime` autocmd on `FocusGained,BufEnter,CursorHold,CursorHoldI` for instant buffer reloading when Pi changes files on disk.
- Mapped `<leader>ao` to open the terminal, and `<leader>ap` to send file (normal mode) or selection (visual mode).
- Cleaned which-key `<leader>a` group to `ai (pi)` with descriptions for `<leader>ao` and `<leader>ap`.
- Staged and pushed `~/.config/nvim/init.lua` and `~/.tmux.conf` to `~/.dotfiles.git`.

## Constraints & Rules
- Preserved custom self-contained package manager `use()`.
- Zero errors / warnings during headless Neovim bootstrap.

## Files touched
- `~/.config/nvim/init.lua`
- `~/.tmux.conf`
- `docs/current/pi-nvim.md`

## Next recommended step
- All actions complete.
